from django.apps import AppConfig

"""
This module defines configuration parameters for the Files app.
"""

class FilesConfig(AppConfig):
    """AppConfig class for the files application."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "files"
